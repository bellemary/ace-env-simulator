# 更新日志

## 1.1
- 基础模式移除目标环境快照、目标文件、进程栈、maps、namespace、上下文和 mounts 槽，只执行可独立复现的客户端环境项目。
- `ap_ker`/`ksu_mtp` 页驻留探针在运行时配置门未知时不再显示为通过。
- 高级模式增加 KSU SELinux、BL/Verified Boot、设备指纹属性观察。
- 高级模式导出原始文件副本、MRPCS/a64/ano_rdp/MPMC/comm 解码产物，并支持报告与源文件 ZIP。

## 1.0

- 首次发布基础环境逐项检测。
- 首次发布 Root 只读设备画像文件解析。
- 支持基础与高级两类独立报告。
# v2.0

- 高级模式改为会话式流程：Root 验证后启动目标应用，后台前台服务记录进程生命周期、相关文件哈希变化和变化文件快照。
- 返回应用后自动停止会话，解码当前文件与运行期间变化快照，并将时间线和原始副本放入高级报告包。
- 严格区分静态规则覆盖范围与本机明确观察值；MRPCS 中出现 KernelSU 或工具扫描字符串不再计作本机命中。
- 修复 Android `unzip` 文件清单兼容性，补充 `kmc.dat` 与 `mn_cache.dat` 的结构化解析。
- 高级采集保持只读，不包含目标内存写入、进程隐藏或渲染数据修改。
# v2.1

- 高级采集改为手动结束：返回本应用后继续采集，可反复进入目标应用，点击“结束并生成报告”后才停止。
- 新增持续采集状态面板、继续进入目标应用按钮和手动结束按钮。
- 高级结果统一为“扫描来源、扫描了、发现了、设备判定、上报结论”。
- 将 `TssSDKGetReportData`、`tss_get_report_data2`、`tss_get_report_data3` 分别加入运行时接口台账，接口存在与实际报告负载分开判定。
- 报告包名称更新为“高级Ace运行会话与上报分析报告”。
